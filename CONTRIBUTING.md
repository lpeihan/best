# Style rules

```javascript
/* in a top level of file */
const COMMENT_MAX_LENGTH = 64;

enum CreditCardType {
  AmericanExpress = 'AMERICAN_EXPRESS',
  Mastercard = 'MASTERCARD',
  Visa = 'VISA',
}

/* Use trailing comma */
const foo = [
  'bar',
  'baz',
  'foo',
]
```
